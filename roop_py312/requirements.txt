--extra-index-url https://download.pytorch.org/whl/cu118

# 基础依赖
numpy>=1.26.0,<2.0.0
opencv-python-headless>=4.9.0.80
pillow>=10.2.0
protobuf>=4.25.1
psutil>=5.9.8
tqdm>=4.66.1

# 深度学习/推理框架
torch>=2.2.0
torchvision>=0.17.0
onnx>=1.15.0
onnxruntime>=1.17.0; sys_platform != 'darwin'
onnxruntime-gpu>=1.17.0; sys_platform != 'darwin'  # GPU 加速 Linux/Windows
onnxruntime-silicon>=1.17.0; sys_platform == 'darwin' and platform_machine == 'arm64'
onnxruntime-coreml>=1.17.0; sys_platform == 'darwin' and platform_machine != 'arm64'

# InsightFace (face swap)
insightface>=0.7.3

# GFPGAN (face enhance)
git+https://github.com/TencentARC/GFPGAN.git

# TensorFlow (可选，如果不用可以删掉)
tensorflow>=2.15.0
